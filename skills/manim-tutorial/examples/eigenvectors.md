# Eigenvectors

Show a grid transformation and several arrows before defining eigenvectors. Highlight a direction that remains on the same line while its length changes. Only then introduce `A v = λ v`, connecting the scalar to the observed stretch. Contrast one non-eigen direction to prevent the misconception that every vector only stretches.
