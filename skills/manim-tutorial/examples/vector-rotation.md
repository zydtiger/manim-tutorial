# Vector rotation

Open with an arrow and ask what can change under a rotation. Rotate the same arrow about the origin, leave a faint reference copy if helpful, and then compare its unchanged length. Only after the visual evidence, name rotation as a length-preserving transformation. Use a short recap caption: "Rotation preserves length."
