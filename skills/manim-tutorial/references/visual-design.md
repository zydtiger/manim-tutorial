# Visual design

Give colors stable meanings, such as one color for an input and another for its transformed counterpart. Preserve objects across steps when possible. Maintain a clear hierarchy: the current object and claim should dominate, while context is muted. Leave margins and avoid shrinking mathematical text to fit too much.
